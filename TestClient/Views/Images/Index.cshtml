@using TestClient.Models
@using System
@model List<TestClient.Models.Image>

@if (Model.Count == 0)
{
  <p>There are no images yet.</p>
}

@foreach (Image image in Model)
{
  var imgBase64 = Convert.ToBase64String(image.Data);
  <img src="data:image/jpg;base64, @imgBase64" />
  <br />
}